import { EventModel } from '@/types/models'
import React from 'react'

type Props = {
  events: EventModel[]
}

export default function Hero({}: Props) {
  return (
    <section>
      <h2>Hero</h2>
    </section>
  )
}
